

<div class="timeline-wrapper">
    <div class="timeline-line"></div>

    <ol class="timeline-list">
        <li class="timeline-item opacity-0" data-index="0">
            <div class="timeline-marker">
                <div class="marker-ring"></div>
                <div class="marker-dot">
                    <svg class="w-3 h-3 text-orange-500" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M20 4a2 2 0 0 0-2-2h-2V1a1 1 0 0 0-2 0v1h-3V1a1 1 0 0 0-2 0v1H6V1a1 1 0 0 0-2 0v1H2a2 2 0 0 0-2 2v2h20V4ZM0 18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8H0v10Zm5-8h10a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Z"/>
                    </svg>
                </div>
            </div>

            <div class="timeline-content-wrapper">
                <div class="timeline-card">
                    <div class="card-header">
                        <h3 class="company-name">OHMYBOX</h3>
                        <span class="current-badge">
                            <span class="badge-dot"></span>
                            Actualidad
                        </span>
                    </div>
                    <time class="time-period">Desde octubre 2025</time>
                    <p class="description">
                        Desarrollo y mantengo OhMyBox, mi e-commerce de alimentos proteicos en producción. Uso Claude Code como herramienta de desarrollo, pero el proyecto requiere gestión completa: arquitectura, decisiones técnicas, flujo de datos y buenas prácticas. Stack: Next.js, PostgreSQL, Prisma, desplegado en Vercel.
                    </p>
                </div>
            </div>
        </li>

        <li class="timeline-item opacity-0" data-index="1">
            <div class="timeline-marker">
                <div class="marker-ring"></div>
                <div class="marker-dot">
                    <svg class="w-3 h-3 text-orange-500" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M20 4a2 2 0 0 0-2-2h-2V1a1 1 0 0 0-2 0v1h-3V1a1 1 0 0 0-2 0v1H6V1a1 1 0 0 0-2 0v1H2a2 2 0 0 0-2 2v2h20V4ZM0 18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8H0v10Zm5-8h10a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Z"/>
                    </svg>
                </div>
            </div>

            <div class="timeline-content-wrapper">
                <div class="timeline-card">
                    <div class="card-header">
                        <h3 class="company-name">Innovateproweb</h3>
                        <span class="ended-badge">Finalizado</span>
                    </div>
                    <time class="time-period">Abril 2024 - Abril 2025</time>
                    <p class="description">
                        Frontend Team Leader en una startup enfocada en desarrollo web para PyMEs. Lideré proyectos frontend con React y coordiné un equipo pequeño para entregar soluciones digitales a clientes.
                    </p>
                </div>
            </div>
        </li>

        <li class="timeline-item opacity-0" data-index="2">
            <div class="timeline-marker">
                <div class="marker-ring"></div>
                <div class="marker-dot">
                    <svg class="w-3 h-3 text-orange-500" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M20 4a2 2 0 0 0-2-2h-2V1a1 1 0 0 0-2 0v1h-3V1a1 1 0 0 0-2 0v1H6V1a1 1 0 0 0-2 0v1H2a2 2 0 0 0-2 2v2h20V4ZM0 18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8H0v10Zm5-8h10a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Z"/>
                    </svg>
                </div>
            </div>

            <div class="timeline-content-wrapper">
                <div class="timeline-card">
                    <div class="card-header">
                        <h3 class="company-name">No Country</h3>
                        <span class="ended-badge">Finalizado</span>
                    </div>
                    <time class="time-period">Febrero 2024 - Abril 2024</time>
                    <p class="description">
                        Simulaciones laborales colaborativas para ganar experiencia en entornos de trabajo IT reales. Trabajé con equipos multidisciplinarios (UX/UI, QA, Backend, Frontend) en proyectos como Querida Alergia y MedConnect.
                    </p>
                </div>
            </div>
        </li>

        <li class="timeline-item opacity-0" data-index="3">
            <div class="timeline-marker">
                <div class="marker-ring"></div>
                <div class="marker-dot">
                    <svg class="w-3 h-3 text-orange-500" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M20 4a2 2 0 0 0-2-2h-2V1a1 1 0 0 0-2 0v1h-3V1a1 1 0 0 0-2 0v1H6V1a1 1 0 0 0-2 0v1H2a2 2 0 0 0-2 2v2h20V4ZM0 18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8H0v10Zm5-8h10a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Z"/>
                    </svg>
                </div>
            </div>

            <div class="timeline-content-wrapper">
                <div class="timeline-card">
                    <div class="card-header">
                        <h3 class="company-name">Freelance Frontend Developer</h3>
                        <span class="current-badge">
                            <span class="badge-dot"></span>
                            Actualidad
                        </span>
                    </div>
                    <time class="time-period">Desde noviembre 2023</time>
                    <p class="description">
                        Desarrollo web freelance para clientes particulares y pequeños negocios. Proyectos variados desde landing pages hasta aplicaciones web, trabajando principalmente con React, Next.js y Astro.
                    </p>
                </div>
            </div>
        </li>
    </ol>
</div>

<style>
/* Contenedor principal */
.timeline-wrapper {
    position: relative;
    padding: 2rem 0;
}

/* Línea vertical central */
.timeline-line {
    position: absolute;
    left: 20px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(
        180deg,
        rgba(249, 115, 22, 0) 0%,
        rgba(249, 115, 22, 0.4) 10%,
        rgba(249, 115, 22, 0.3) 90%,
        rgba(249, 115, 22, 0) 100%
    );
    transform-origin: top;
    animation: drawTimeline 2s ease-out forwards;
    z-index: 0;
}

/* Lista de timeline */
.timeline-list {
    position: relative;
    list-style: none;
    padding: 0;
    margin: 0;
    z-index: 1;
}

/* Item individual */
.timeline-item {
    position: relative;
    padding-left: 60px;
    margin-bottom: 3rem;
    transition: all 0.3s ease;
}

.timeline-item:last-child {
    margin-bottom: 0;
}

/* Marcador (icono + anillo) */
.timeline-marker {
    position: absolute;
    left: 0;
    top: 8px;
    width: 42px;
    height: 42px;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
}

.marker-ring {
    position: absolute;
    width: 42px;
    height: 42px;
    border-radius: 50%;
    background: rgba(17, 24, 39, 0.8);
    border: 2px solid rgba(249, 115, 22, 0.3);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(4px);
}

.marker-dot {
    position: relative;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(31, 41, 55, 0.9), rgba(17, 24, 39, 0.95));
    border: 2px solid rgba(249, 115, 22, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 1;
}

/* Hover effects en el marcador */
.timeline-item:hover .marker-ring {
    border-color: rgba(249, 115, 22, 0.6);
    box-shadow: 0 0 20px rgba(249, 115, 22, 0.3);
    transform: scale(1.1);
}

.timeline-item:hover .marker-dot {
    border-color: rgba(249, 115, 22, 0.9);
    box-shadow: 0 0 15px rgba(249, 115, 22, 0.4);
    transform: scale(1.15);
}

/* Wrapper del contenido */
.timeline-content-wrapper {
    position: relative;
}

/* Card de contenido */
.timeline-card {
    background: linear-gradient(
        135deg,
        rgba(31, 41, 55, 0.4) 0%,
        rgba(17, 24, 39, 0.5) 100%
    );
    border: 1px solid rgba(75, 85, 99, 0.3);
    border-radius: 0.75rem;
    padding: 1.5rem;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(10px);
}

.timeline-card::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 0.75rem;
    padding: 1px;
    background: linear-gradient(
        135deg,
        transparent,
        rgba(249, 115, 22, 0.2),
        transparent
    );
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.4s ease;
}

.timeline-item:hover .timeline-card {
    background: linear-gradient(
        135deg,
        rgba(31, 41, 55, 0.5) 0%,
        rgba(17, 24, 39, 0.6) 100%
    );
    border-color: rgba(249, 115, 22, 0.3);
    transform: translateX(4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
}

.timeline-item:hover .timeline-card::before {
    opacity: 1;
}

/* Header del card */
.card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.75rem;
    gap: 1rem;
    flex-wrap: wrap;
}

.company-name {
    font-size: 1.125rem;
    font-weight: 600;
    color: rgb(249, 115, 22);
    margin: 0;
    line-height: 1.4;
}

/* Badges */
.current-badge,
.ended-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
    white-space: nowrap;
}

.current-badge {
    background: rgba(34, 197, 94, 0.15);
    color: rgb(134, 239, 172);
    border: 1px solid rgba(34, 197, 94, 0.3);
}

.ended-badge {
    background: rgba(239, 68, 68, 0.1);
    color: rgba(248, 113, 113, 0.8);
    border: 1px solid rgba(239, 68, 68, 0.2);
}

.badge-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgb(134, 239, 172);
    animation: pulse-badge 2s ease-in-out infinite;
}

/* Período de tiempo */
.time-period {
    display: block;
    font-size: 0.875rem;
    color: rgba(156, 163, 175, 0.9);
    margin-bottom: 1rem;
    font-weight: 400;
}

/* Descripción */
.description {
    font-size: 0.9375rem;
    line-height: 1.7;
    color: rgba(209, 213, 219, 0.8);
    margin: 0;
}

/* Animaciones */
@keyframes drawTimeline {
    from {
        transform: scaleY(0);
    }
    to {
        transform: scaleY(1);
    }
}

@keyframes pulse-badge {
    0%, 100% {
        opacity: 1;
        transform: scale(1);
    }
    50% {
        opacity: 0.6;
        transform: scale(1.2);
    }
}

.timeline-item.visible {
    animation: fadeInLeft 0.6s ease-out forwards;
}

@keyframes fadeInLeft {
    from {
        opacity: 0;
        transform: translateX(-30px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Responsive */
@media (max-width: 768px) {
    .timeline-wrapper {
        padding: 1rem 0;
    }

    .timeline-line {
        left: 16px;
    }

    .timeline-item {
        padding-left: 50px;
        margin-bottom: 2rem;
    }

    .timeline-marker {
        width: 36px;
        height: 36px;
    }

    .marker-ring {
        width: 36px;
        height: 36px;
    }

    .marker-dot {
        width: 20px;
        height: 20px;
    }

    .marker-dot svg {
        width: 10px;
        height: 10px;
    }

    .timeline-card {
        padding: 1rem;
    }

    .company-name {
        font-size: 1rem;
    }

    .timeline-item:hover .timeline-card {
        transform: translateX(2px);
    }

    .timeline-item:hover .marker-ring {
        transform: scale(1.05);
    }

    .timeline-item:hover .marker-dot {
        transform: scale(1.1);
    }
}

@media (max-width: 640px) {
    .card-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }
}
</style>

<script>
    // Intersection Observer para animación de entrada
    const observerOptions = {
        root: null,
        rootMargin: '0px',
        threshold: 0.2
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) {
                const index = entry.target.getAttribute('data-index');
                const delay = index ? parseInt(index) * 150 : 0;
                setTimeout(() => {
                    entry.target.classList.add('visible');
                }, delay);
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    document.addEventListener('DOMContentLoaded', () => {
        const items = document.querySelectorAll('.timeline-item');
        items.forEach(item => observer.observe(item));
    });
</script>
