---
import  projects  from "../mocs/projects.js";
console.log(projects);
import ProjectCard from "./ProjectCard.astro"
---

{
    projects?.map(project =>{
        return ( <ProjectCard project={project}/> )
    })
} 